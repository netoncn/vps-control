# ===========================================
# Configuracao do Servidor (este painel)
# ===========================================

# Host do servidor (0.0.0.0 para producao, 127.0.0.1 para dev local)
HOST=0.0.0.0

# Porta do servidor
PORT=4000

# URL do frontend para CORS (em producao, usar URL real)
CORS_ORIGIN=http://localhost:5173

# ===========================================
# Autenticacao (opcional, mas recomendado em producao)
# ===========================================

# Habilitar autenticacao (true/false)
AUTH_ENABLED=false

# Segredo JWT (gerar um valor aleatorio forte para producao)
# Exemplo: openssl rand -base64 32
AUTH_JWT_SECRET=change-this-secret-in-production

# Usuario para login
AUTH_USERNAME=admin

# Hash da senha (gerar com: node -e "require('bcryptjs').hash('sua-senha', 10).then(console.log)")
# Exemplo abaixo e para a senha "admin123" - MUDE EM PRODUCAO!
AUTH_PASSWORD_HASH=$2a$10$rQZ8K.0wR8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K8K

# ===========================================
# Configuracao SSH da VPS Gerenciada
# ===========================================

# IP ou hostname da VPS que sera gerenciada
VPS_HOST=seu.servidor.com

# Porta SSH (padrao: 22)
VPS_PORT=22

# Usuario SSH
VPS_USERNAME=root

# Caminho para chave privada SSH
# Linux/Mac: /home/user/.ssh/id_rsa
# Windows: C:\Users\User\.ssh\id_rsa
VPS_PRIVATE_KEY_PATH=/path/to/id_rsa

# Passphrase da chave (se houver)
# VPS_PRIVATE_KEY_PASSPHRASE=

# Senha SSH (evitar - usar chave SSH)
# VPS_PASSWORD=

# Timeout de conexao SSH em ms
SSH_CONNECT_TIMEOUT_MS=15000
